# === Telegram Bot ===
# Obtén el token en @BotFather y tu ID en @userinfobot
TELEGRAM_TOKEN=TU_TELEGRAM_TOKEN
CREATOR_ID=123456789

# (Opcional) URL del backend si lo usas desde el bot
BACKEND_URL=http://sigha-ing-sistemas-back-preprod:8081

# === Base de Datos PostgreSQL ===
# Host del contenedor de Postgres dentro de la red Docker
DB_HOST=sigha-db-preprod
# Puerto interno del servicio Postgres (normalmente 5432)
DB_PORT=5432
# Credenciales de la BD
DB_USER=admin
DB_PASSWORD=tu_password_seguro
# Nombre de la base de datos
DB_NAME=sigha
# Usa true si tu Postgres exige SSL; de lo contrario false
DB_SSL=false